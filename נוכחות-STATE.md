# נוכחות-STATE — מצב נוכחי (עדכון אחרון: 15/07/2026, ערב)

## ✅ עובד ומאומת
- הצינור המלא: טפסים → "נתונים" → טאב 3. אומת ע"י זיו על נתוני יוני אמיתיים.
- [P] מעון הוד: כל השמות, התאריכים והספירות נכונים.
- [A] מעון הוד-טירה: נכנס דרך "רשימות" (מזל ד, דני, אסף) — 4 תאריכים.
- טריגר עריכה בטאב 3 → ריענון נוסחאות (רעיה נוספה ונקלטה אונליין).
- רשימות: קבוצה=מזל ד,דני,אסף · פרטני=מזל ד,דני,אסף.

## ⚠️ פער קריטי אחד: גרסת הקוד בגיליון ישנה
**ההדבקה האחרונה לא נקלטה אצל זיו.** הקובץ `נוכחות.gs` בתיקייה הוא מקור האמת והוא מקדים את מה שמותקן. מה שחסר בגיליון עד ההדבקה הבאה:
- עריכה ב"רשימות" → בנייה מחדש אוטומטית (כרגע: ידני, תפריט נוכחות ← בנייה מחדש).
- דשבורד בעמודה A של טאב 3 (+ הרחבת עמודה).
- חוזה שורות 35/36/37 (הגרסה המותקנת כנראה עם בקרה בשורה 41 או בלעדיה).
**פעולה**: הדבקה מלאה (Ctrl+A,V,S בעורך) של `נוכחות.gs` + מחיקת הטאב "דשבורד" הישן שנוצר מגרסת ביניים.

## 🔲 פתוח
1. ההדבקה הסופית (למעלה).
2. טאב "מקומות": אימיילים למדריכים + שורות למעון הוד ולטירה (ראו `מזכירה-אוטומטית.md`) — בלעדיהן אין תזכורות.
3. הודיה + אסתר גרוס סומנו בפרטני בטופס אך אין להן שורה בעמודת AB בטאב 3 (זיו סידר מחדש — לאמת).
4. קוסמטי: A1 בטאב 3 = "והיא" במקום "בחור חודש:".
5. הקפאת פיצ'רים אחרי ההדבקה — תפעול בלבד.

## איך מאמתים מהצד שלנו (בלי גישת כתיבה)
Drive MCP ← download_file_content בייצוא xlsx ← פענוח ה-zip בסקריפטי PowerShell שבסקretchpad (inspect3/inspidx — לשכתב אם אבדו: פורקים xl/workbook.xml + sharedStrings). נתיב עם שם משתמש עברי: להשתמש ב-`C:\Users\80FD~1\…` בפרמטרים.
העברת קוד לזיו: `Set-Clipboard` על תוכן `נוכחות.gs` (עובד), או הדפסת הקוד המלא בצ'אט (גיבוי אמין). גישה ישירה ל-script.google.com חסומה לכל כלי הדפדפן.

## תיאום עם העוזרת (נבדק 15/07 — קראתי את backend/Code.gs במלואו)
- אין התנגשות: גיליונות נפרדים; היומן משותף בכיוונים משלימים (היא כותבת, אנחנו קוראים).
- טריגר התזכורות של הנוכחות (20:00) פעיל; הטריגרים של העוזרת עוד לא הופעלו (PLAN #1 שלה).
- יעד עתידי: כשייבנה מנוע התזכורות הגנרי של העוזרת (PLAN #6), תזכורת הנוכחות עוברת לשם ו-sendReminders מכובה. עד אז — נשארת בנוכחות.gs. פרטים: מזכירה-אוטומטית.md.
